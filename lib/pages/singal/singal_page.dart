import 'package:flutter/material.dart';

class SingalPage extends StatelessWidget {
  const SingalPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
